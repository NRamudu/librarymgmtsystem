# application-test.yml
spring:
    datasource:
        url: jdbc:mysql://localhost:3306/library_test?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
        username: root
        password: admin
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
